import { Module } from '@nestjs/common';

/**
 * Esqueleto do Bounded Context de Templates (secao 7 do AGENTS.md).
 * Estrutura domain/application/infrastructure/presentation preparada;
 * casos de uso ainda nao implementados nesta entrega.
 */
@Module({})
export class TemplatesModule {}
